

{$DEFINE WL_MOUSEZOOM_INCLUDED}
{$INCLUDE_ONCE WaspLib/main.simba}

type

  TMouseZoom = record
    ZoomLevel: Integer;
    Enabled, SettingChecked: Boolean;
    const MAX_ZOOM: Integer = 768;
  end;


function TMouseZoom.GetNext(value: Integer; down: Boolean): Integer;
begin
  if down then Exit(Max(0, value-26));
  Result := Min(value+24, Self.MAX_ZOOM);
end;

function TMouseZoom.GetNext(value: Integer; amount: Integer = 1; down: Boolean = True): Integer; overload;
var
  i: Integer;
begin
  Result := value;
  for i := 1 to amount do
    Result := Self.GetNext(Result, down);
end;


function TMouseZoom.Level2Slider(value: Integer = -1): Integer;
begin
  if value < 0 then
  begin
    value := Options.GetZoomLevel();
    if value < 0 then Exit;
  end;

  Result := Floor(value/8);

  //The following levels do not exist on the slider, probably due to roundings.
  //The scale of 0-100 is made up by us so we are bound to rounding issues.
  if Result >= 13 then Result += 1;
  if Result >= 37 then Result += 1;
  if Result >= 63 then Result += 1;
  if Result >= 87 then Result += 1;
end;


function TMouseZoom.Slider2Level(value: Integer = -1): Integer;
begin
  if value < 0 then
  begin
    value := Options.GetZoomLevel();
    if value < 0 then Exit;
  end;

  //handling "dead" levels of the zoom slider.
  //Despite we using a 0..100 range, the slider only has 97 positions.
  case value of
    13.. 36: Dec(value);
    37.. 62: Dec(value, 2);
    63.. 86: Dec(value, 3);
    87..100: Dec(value, 4);
  end;

  Result := value * 8;
end;



procedure TMouseZoom.Scroll(amount: Integer = 1; down: Boolean; forceMove: Boolean = True);
{$IFNDEF WL_DISABLE_FAKE_INPUT}
var
  inputState: Boolean;
{$ENDIF}
begin
  Self.ZoomLevel := Self.GetNext(Self.ZoomLevel, amount, down);
  Options.ZoomLevel := Self.Level2Slider(Self.ZoomLevel);

  {$IFNDEF WL_DISABLE_FAKE_INPUT}
  inputState := Client.IsInputEnabled();
  if inputState then Client.DisableRealInput();
  {$ENDIF}

  Mouse.Scroll(MSInterface.Bounds, amount, down, forceMove);

  {$IFNDEF WL_DISABLE_FAKE_INPUT}
  if inputState then Client.EnableRealInput();
  {$ENDIF}
end;

procedure TMouseZoom.Scroll(amount: Integer = 1; forceMove: Boolean = True); overload;
{$IFNDEF WL_DISABLE_FAKE_INPUT}
var
  inputState: Boolean;
{$ENDIF}
begin
  {$IFNDEF WL_DISABLE_FAKE_INPUT}
  inputState := Client.IsInputEnabled();
  if inputState then Client.DisableRealInput();
  {$ENDIF}

  Self.Scroll(Floor(Sqrt(Power(amount,2))), amount < 0, forceMove);

  {$IFNDEF WL_DISABLE_FAKE_INPUT}
  if inputState then Client.EnableRealInput();
  {$ENDIF}
end;



procedure TMouseZoom.MaxZoom(down: Boolean = True);
var
  scrolls: Integer;
begin
  if down then scrolls := Floor(Self.ZoomLevel/26) + Random(3, 6)
  else scrolls := Floor((Self.MAX_ZOOM - Self.ZoomLevel)/24) + Random(3, 6);
  Self.Scroll(scrolls, down, True);
end;


procedure TMouseZoom.MaxZoomBlind(down: Boolean = True);
var
  scrolls: Integer;
begin
  if down then
    scrolls := Floor(Self.MAX_ZOOM/26) + Random(3, 6)
  else
    scrolls := Floor(Self.MAX_ZOOM/24) + Random(3, 6);

  Self.Scroll(scrolls, down, True);
end;


function TMouseZoom.GetZoomLevel(): Integer;
begin
  if not Self.SettingChecked then
    if not Options.OpenTab(EOptionsTab.DISPLAY) then
      Exit;

  if (Self.ZoomLevel > -1) then
    Exit(Self.ZoomLevel);

  if (Options.ZoomLevel > -1) then
  begin
    Self.ZoomLevel := Self.Slider2Level(Options.ZoomLevel);
    Exit(Self.ZoomLevel);
  end;

  case Random(100) of
    0: Options.SetZoomLevel(Random(101));
    1..30: Self.MaxZoomBlind(Biometrics.RandomBoolean(0.5));
    31..99: Self.ZoomLevel := Self.Slider2Level(Options.GetZoomLevel());
  end;

  Result := Self.ZoomLevel;

  if Result = -1 then
    DebugLn('RSMouseZoom', 'Failed to read zoom!', ELogLevel.ERROR)
  else
    DebugLn('RSMouseZoom', 'Current zoom level: ' + ToStr(Options.ZoomLevel), ELogLevel.SUCCESS);
end;



function TMouseZoom.SetZoomLevel(level: Integer): Boolean;
var
  rslevel, amount: Integer;
begin
  if not Self.SettingChecked then
    if not Options.OpenTab(EOptionsTab.DISPLAY) then
      Exit;

  if not Self.Enabled then
    Exit(Options.SetZoomLevel(level));

  if Self.ZoomLevel = -1 then
  begin
    Self.GetZoomLevel();

    if Self.ZoomLevel = -1 then
      raise GetDebugLn('RSMouseZoom', 'Something went wrong while setting up the zoom level.');
  end;

  rslevel := Self.Slider2Level(level);

  if InRange(rslevel, Self.ZoomLevel-26, Self.ZoomLevel+24) then
    Self.MaxZoom(Biometrics.RandomBoolean());

  amount := rslevel - Self.ZoomLevel;

  if Self.ZoomLevel > rslevel then
    Self.Scroll(Floor(amount/26))
  else
    Self.Scroll(Floor(amount/24));

  Result := InRange(Options.ZoomLevel, level-4, level+4);

  if Result then
    DebugLn('RSMouseZoom', 'Current zoom level set to: ' + ToString(Options.ZoomLevel), ELogLevel.SUCCESS)
  else
    DebugLn('RSMouseZoom', 'Failed to set zoom! Current level: ' + ToString(Options.ZoomLevel), ELogLevel.ERROR);
end;

var

  RSMouseZoom: TMouseZoom;
